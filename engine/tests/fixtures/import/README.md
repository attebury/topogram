These source fixtures are extracted from the older local trial corpus so the
engine regression suite can keep a few realistic importer inputs without
requiring the full upstream repos in `trials/`.

They are intentionally small, repo-contained test inputs rather than public
proof targets. Imported proof claims now live in `topogram-demo`.
