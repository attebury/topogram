# imported confidence: medium
# imported source_kind: migration
# imported provenance: migrations/002.do.tasks.sql
entity entity_task {
  name "Task"
  description "Candidate entity imported from brownfield schema evidence"

  fields {
    id int required
    name varchar_255 required
    author_id int required
    assigned_user_id int optional
    filename varchar_255 optional
    status varchar_50 required
    created_at timestamp optional
    updated_at timestamp optional
  }

  keys {
    primary [id]
  }

  status active
}
