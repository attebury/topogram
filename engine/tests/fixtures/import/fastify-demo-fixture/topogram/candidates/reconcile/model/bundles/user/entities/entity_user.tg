# imported confidence: medium
# imported source_kind: migration
# imported provenance: migrations/001.do.users.sql
entity entity_user {
  name "User"
  description "Candidate entity imported from brownfield schema evidence"

  fields {
    id int required
    email varchar_255 required
    username varchar_255 required
    password varchar_255 required
    created_at timestamp optional
    updated_at timestamp optional
  }

  keys {
    primary [id]
    unique [email]
  }

  status active
}
