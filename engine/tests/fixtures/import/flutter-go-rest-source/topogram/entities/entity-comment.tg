# imported confidence: high
# imported source_kind: schema
# imported provenance: #10 - Clean Architecture Version (RxDart + Provider)/lib/features/comment/domain/entities/comment_entity.dart
entity entity_comment {
  name "Comment"
  description "Candidate entity imported from brownfield schema evidence"

  fields {
    id int required
    postId int required
    name string required
    email string required
    body string required
  }

  keys {
    primary [id]
  }

  status active
}
