# imported confidence: high
# imported source_kind: schema
# imported provenance: #10 - Clean Architecture Version (RxDart + Provider)/lib/features/user/domain/entities/user_entity.dart
entity entity_user {
  name "User"
  description "Candidate entity imported from brownfield schema evidence"

  fields {
    id int optional
    name string required
    email string required
    gender gender required
    status userstatus required
  }

  keys {
    primary [id]
  }

  status active
}
