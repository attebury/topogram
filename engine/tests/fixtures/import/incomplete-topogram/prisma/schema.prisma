generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

enum task_priority {
  low
  medium
  high
}

model Task {
  id        String        @id @default(uuid())
  title     String
  priority  task_priority @default(medium)
}
