# imported confidence: high
# imported source_kind: schema
# imported provenance: engine/tests/fixtures/import/nextjs-graphql-source/prisma/schema.prisma
entity entity_user {
  name "User"
  description "Candidate entity imported from brownfield schema evidence"

  fields {
    email string required
    id int required
    name string optional
  }

  keys {
    primary [id]
    unique [email]
  }

  status active
}
