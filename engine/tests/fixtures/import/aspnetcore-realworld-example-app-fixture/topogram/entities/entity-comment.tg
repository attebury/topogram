# imported confidence: high
# imported source_kind: schema
# imported provenance: src/Conduit/Domain/Comment.cs
entity entity_comment {
  name "Comment"
  description "Candidate entity imported from brownfield schema evidence"

  fields {
    commentId int required
    body string optional
    authorId int required
    articleId int required
    createdAt datetime required
    updatedAt datetime required
  }

  keys {
    primary [commentId]
  }

  status active
}
