# imported confidence: high
# imported source_kind: schema
# imported provenance: src/main/java/com/delivery/data/db/jpa/entities/CousineData.java
entity entity_cousine {
  name "Cousine"
  description "Candidate entity imported from brownfield schema evidence"

  fields {
    id long required
    name string required
  }

  keys {
    primary [id]
    unique [name]
  }

  status active
}
