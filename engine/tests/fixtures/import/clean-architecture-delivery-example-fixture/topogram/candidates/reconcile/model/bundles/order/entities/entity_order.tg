# imported confidence: high
# imported source_kind: schema
# imported provenance: src/main/java/com/delivery/data/db/jpa/entities/OrderData.java
entity entity_order {
  name "Order"
  description "Candidate entity imported from brownfield schema evidence"

  fields {
    id long required
    customer_id customerdata required
    store_id storedata required
    total double required
    status status required
    created_at instant required
    updated_at instant required
  }

  keys {
    primary [id]
  }

  status active
}
