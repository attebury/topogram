# imported confidence: high
# imported source_kind: route_code
# imported provenance: src/main/java/com/delivery/presenter/rest/api/store/StoreResource.java#GET /Store/search/{text}
capability cap_search_stores {
  name "Search Stores"
  description "Candidate capability imported from brownfield API evidence"

  reads [entity_store]

  status active
}
