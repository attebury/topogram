# imported confidence: high
# imported source_kind: schema
# imported provenance: src/main/java/com/delivery/data/db/jpa/entities/ProductData.java
entity entity_product {
  name "Product"
  description "Candidate entity imported from brownfield schema evidence"

  fields {
    id long required
    name string required
    description string required
    price double required
    store_id storedata required
  }

  keys {
    primary [id]
    unique [name]
  }

  status active
}
