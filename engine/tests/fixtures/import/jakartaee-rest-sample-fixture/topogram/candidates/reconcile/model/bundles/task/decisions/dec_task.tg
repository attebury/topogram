# imported confidence: medium
# imported source_kind: generated_artifact
# imported provenance: src/main/java/com/example/interfaces/task/TaskResource.java#GET /tasks/{id}
decision dec_task {
  name "Task Workflow"
  description "Candidate workflow decision imported from brownfield evidence"

  context [cap_create_task, cap_delete_task]
  consequences [created, deleted]

  status proposed
}
