# imported confidence: high
# imported source_kind: schema
# imported provenance: orm/graphql-sdl-first/prisma/schema.prisma
entity entity_post {
  name "Post"
  description "Candidate entity imported from brownfield schema evidence"

  fields {
    id int required
    createdAt datetime required
    updatedAt datetime required
    title string required
    content string optional
    published boolean required
    viewCount int required
    authorId int optional
  }

  keys {
    primary [id]
  }

  status active
}
