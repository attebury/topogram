rule rule_only_active_users_may_be_assigned_issues {
  name "Only active users may be assigned issues"
  description "An issue assignee must be an active user"

  applies_to [cap_create_issue, cap_update_issue]
  requirement "assignee.is_active == true"
  severity error

  status active
}
