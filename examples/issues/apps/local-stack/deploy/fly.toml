app = "topogram-issues-deployment-stack"
primary_region = "ord"

[build]
  dockerfile = "server/Dockerfile"

[env]
  PORT = "3000"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
