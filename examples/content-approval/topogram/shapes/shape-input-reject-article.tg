shape shape_input_reject_article {
  name "Reject Article Input"
  description "Input for rejecting an article under review"

  fields {
    article_id uuid required
    rejected_at datetime optional
    reviewer_notes text optional
  }

  status active
}
