# Content Approval Implementation

This folder holds Content Approval-specific implementation details that stay out of the generic engine.

It now owns the example-specific pieces that make the generated Content Approval runtimes concrete:

- backend seed/reference data
- repository renderers
- runtime-check behavior
- React page renderers and UI reference metadata
