# Web Reference Seam

This proof note anchors the maintained web reference output for the `content-approval` example.

It exists so maintained-boundary and seam-oriented query surfaces can treat the implementation-side web reference as a governed output, not only the single `examples/maintained/proof-app/**` maintained runtime.

The important seam here is the example web composition reference:

- maintained file: `examples/generated/content-approval/implementation/web/reference.js`
- emitted dependencies: `proj_web`, `journey_editorial_review_and_revision`
- expected posture: accepted change, contract-bound
