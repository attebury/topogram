acceptance_criterion ac_stateful_action_boundary_enforced {
  name "Stateful Action Boundary Enforced"
  description "Given important command-owned state surfaces exist, when tests and boundary checks run, then bypasses are explicit instead of silent."
  requirement req_stateful_actions_use_cli
  status approved
}
