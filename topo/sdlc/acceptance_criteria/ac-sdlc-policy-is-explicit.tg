acceptance_criterion ac_sdlc_policy_is_explicit {
  name "SDLC Policy Is Explicit"
  description "Given the engine repo has adopted SDLC, when policy checks run, then topogram.sdlc-policy.json exists with status adopted and mode enforced."
  requirement req_enforce_sdlc_operating_habit
  status approved
}
