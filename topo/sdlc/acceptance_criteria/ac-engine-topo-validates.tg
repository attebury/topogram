acceptance_criterion ac_engine_topo_validates {
  name "Engine Topo Validates"
  description "Given the code-derived engine topo exists, when `topogram check` runs from the repository root, then the workspace validates."
  requirement req_engine_topo_code_derived
  status approved
}
